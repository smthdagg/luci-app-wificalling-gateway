name: CI

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-python@v7
        with:
          python-version: '3.12'
      - uses: actions/setup-node@v7
        with:
          node-version: '22'
      - name: Unit and integration tests
        run: python3 -m unittest discover -s tests -v
      - name: Shell syntax
        run: for f in root/etc/init.d/wificalling-gateway root/usr/libexec/wificalling-gateway/*.sh scripts/*.sh; do sh -n "$f"; done
      - name: LuCI JavaScript syntax
        run: for f in htdocs/luci-static/resources/view/wificalling-gateway/*.js; do node --check "$f"; done
      - name: Build package
        run: ./scripts/build-ipk.sh 1.3.0-1
